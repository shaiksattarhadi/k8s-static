apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: static

spec:
  type: LoadBalancer
  selector: 
    app: k8s-static-deploymant
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  
