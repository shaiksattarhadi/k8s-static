apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-static
  namespace: static
spec:
  rules:
  - host: "sw.shaiksattarhadi.com"
    http:
      paths:
      - pathType: 
        path: 
        backend:
          service:
            name: static-service
            port:
              number: 80
  - host: 
    http:
      paths:
      - pathType: 
        path: 
        backend:
          service:
            name: 
            port:
              number: 