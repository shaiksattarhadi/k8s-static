apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-ing
  namespace: static
spec:
  rules:
  - host: "sw.shaiksattarhadi.com"
    http:
      paths:
      
        backend:
          service:
            name: k8s-static-svc
            port:
              number: 80